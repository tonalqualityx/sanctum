server {
    listen 80;
    server_name ssl.local;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name ssl.local;

    ssl_certificate     /home/mike/sanctum/ssl/certs/ssl.local/ssl.local.pem;
    ssl_certificate_key /home/mike/sanctum/ssl/certs/ssl.local/ssl.local-key.pem;

    access_log /var/log/sanctum/sites/ssl.local-access.log main;
    error_log  /var/log/sanctum/sites/ssl.local-error.log warn;

    limit_req zone=sanctum burst=20 nodelay;

    location / {
        proxy_pass http://127.0.0.1:8007;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-For $remote_addr;
    }

    location ~* \.(php|cgi|pl|py|sh)$ {
        return 403;
    }

    location ~ /\. {
        deny all;
    }

    # optional tighter rate limit on admin or login
    location ~* ^/(wp-login\.php|wp-admin/|xmlrpc\.php) {
        limit_req zone=sanctum burst=5 nodelay;
        proxy_pass http://127.0.0.1:8007;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-For $remote_addr;
    }
}